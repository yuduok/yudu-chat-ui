{
  "name": "@yudu/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "build:web": "pnpm --filter @yudu/web build",
    "build:server": "pnpm --filter @yudu/server build && pnpm --filter @yudu/desktop bundle:server",
    "bundle:server": "node ./scripts/bundle-server.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0",
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.1.1",
    "@tauri-apps/plugin-shell": "^2.0.1",
    "@tauri-apps/plugin-fs": "^2.0.3",
    "@tauri-apps/plugin-dialog": "^2.0.1",
    "@tauri-apps/plugin-deep-link": "^2.0.0"
  }
}
